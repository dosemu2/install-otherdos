target("dosemu-freedos")
    set_kind("phony")
    on_install(function (target)
        local installdir = target:installdir()
        local bindir = path.join(installdir, "bin")
        local libexecdir = path.join(installdir, "libexec", "dosemu")
        os.cp("$(projectdir)/src/dosemu-installdos", bindir)
        os.ln(path.join(bindir, "dosemu-installdos"), path.join(libexecdir, "dosemu-installdos"))
        os.cp("$(projectdir)/src/dosemu-install3rdpartydos",  bindir)
        os.cp("$(projectdir)/src/dosemu-installmsdos",  bindir)
        os.cp("$(projectdir)/src/dosemu-installopendos",  bindir)
    end)
    on_uninstall(function (target)
        local installdir = target:installdir()
        local bindir = path.join(installdir, "bin")
        local libexecdir = path.join(installdir, "libexec", "dosemu")
        os.rm(path.join(bindir, "dosemu-installdos"))
        os.rm(path.join(libexecdir, "dosemu-installdos"))
        os.rm(path.join(bindir, "dosemu-install3rdpartydos"))
        os.rm(path.join(bindir, "dosemu-installmsdos"))
        os.rm(path.join(bindir, "dosemu-installopendos"))
    end)
